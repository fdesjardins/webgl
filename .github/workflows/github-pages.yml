name: GitHub Pages

on:
  push:
    branches:
    - v2

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - run: npm run build
    - run: npm run push-gh-pages
