export const meta = {
  tags: 'threejs,gpgpu',
  title: 'GPU Compute Renderer',
  slug: 'gpu-compute',
}

export const options = {
  display: 'fullscreen',
}

export const init = ({ canvas, container }) => {}
